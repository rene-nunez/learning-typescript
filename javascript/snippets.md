# Snippets

Un snippet es un fragmento de código reutilizable que se inserta rápidamente en el editor, como un atajo de teclado para código.

Agregar en settings.json, esto hace que tus snippets aparezcan primero en las sugerencias del editor (Visual Studio Code).

```JSON
"editor.snippetSuggestions": "top"
```